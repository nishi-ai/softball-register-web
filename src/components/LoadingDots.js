import React from "react";
import styled from 'styled-components';
import { BallTriangle } from  'react-loader-spinner'

const LoadingDots = () => {

    return (
        <BallTriangle color="#FFC15A" height={100} width={100}/>
    );
};

export default LoadingDots;